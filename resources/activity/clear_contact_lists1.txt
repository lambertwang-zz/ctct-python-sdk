> constantcontact = ConstantContact(api_key, access_token)
> 
> lists = [constantcontact.get_lists()[0].get_id()]
> 
> response = constantcontact.clear_contact_lists(lists)
> print json.dumps(response, indent = 4, sort_keys = True)
{
    "contact_count": 0, 
    "error_count": 0, 
    "id": "idididid-idid-idid-idid-idididididid",
    "type": "CLEAR_CONTACTS_FROM_LISTS"
}
